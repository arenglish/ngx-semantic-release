{
  "name": "ngx-semantic-release",
  "version": "0.0.1",
  "description": "Semantic release plugin for angular workspaces",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "engines": {
    "node": ">=10.13"
  },
  "scripts": {
    "test": "echo 'no tests right now'",
    "build": "mkdir dist && shopt -s extglob && cp !(node_modules,.circleci) dist/ && ls dist",
    "semantic-release": "semantic-release"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arenglish/ngx-semantic-release.git"
  },
  "keywords": [
    "ngx",
    "angular",
    "semantic-release",
    "semantic",
    "versioning",
    "release",
    "plugin"
  ],
  "author": "Austin R English",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/arenglish/ngx-semantic-release/issues"
  },
  "homepage": "https://github.com/arenglish/ngx-semantic-release#readme",
  "peerDependencies": {
    "semantic-release": ">=15.4.4 <17.0.0",
    "@angular/cli": ">=6.0.0"
  },
  "devDependencies": {
    "@semantic-release/exec": "^4.0.0",
    "@semantic-release/git": "^8.0.0",
    "semantic-release": ">=15.4.4"
  }
}
